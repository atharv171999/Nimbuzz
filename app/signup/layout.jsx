export const metadata = {
  title: "Join Nimbuzz",
  description: "Create your Nimbuzz account and join the next generation of social interaction. Connect, share, and gather with your community.",
};

export default function SignupLayout({ children }) {
  return children;
}
