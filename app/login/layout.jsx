export const metadata = {
  title: "Sign In",
  description: "Sign in to your Nimbuzz account to stay connected with your community and discover the next generation of social interaction.",
};

export default function LoginLayout({ children }) {
  return children;
}
